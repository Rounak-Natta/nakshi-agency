"use client";

export default function About() {
  return (
    <section className="h-screen flex items-center justify-center px-6 bg-[var(--background)] text-[var(--foreground)]">
      <p className="text-2xl md:text-4xl max-w-2xl text-center">
        We craft immersive, high-end digital experiences with motion and precision.
      </p>
    </section>
  );
}